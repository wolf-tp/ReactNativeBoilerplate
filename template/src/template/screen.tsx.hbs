import { View, Text } from "react-native";
import React from "react";

type {{pascalCase inputName}}ScreenProps = {};

export const {{pascalCase inputName}}Screen = (props: {{pascalCase inputName}}ScreenProps) => {
  return (
    <View>
      <Text>{{pascalCase inputName}}</Text>
    </View>
  );
};
