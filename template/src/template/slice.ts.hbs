import { createSlice, PayloadAction } from '@reduxjs/toolkit';

type {{camelCase inputName}}SliceType = {
  randomNumbers: number[];
};
const initState: {{camelCase inputName}}SliceType = {
  randomNumbers: [],
};

export const {{camelCase inputName}} = createSlice({
  initialState: initState,
  name: '{{camelCase inputName}}',
  reducers: {
    //Handle loading when call api
    onRandomNumber: state => {},
    setRandomNumber: (state, action: PayloadAction<number[]>) => {
      state.randomNumbers = action.payload;
    },
  },
});

export const { setRandomNumber, onRandomNumber } = {{camelCase inputName}}.actions;

export const {{camelCase inputName}}Reducer = {{camelCase inputName}}.reducer;
