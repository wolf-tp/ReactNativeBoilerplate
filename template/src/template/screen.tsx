import { View, Text } from "react-native";
import React from "react";

type BaseProps = {};

export const Base = (props: BaseProps) => {
  return (
    <View>
      <Text>Base</Text>
    </View>
  );
};
